import React, { useEffect, useState } from 'react';
import Feed from '../../components/Feed';

const HomeScreen = ({ refreshFeed, setRefreshFeed }) => {
  return <Feed />;
};

export default HomeScreen;